<div class="container">
  <h1>Leagues of roller derby - EUROPE</h1>
  <% @leagues.each do |league| %>
    <img src="<%= league.logo %>" alt="">
    <h2><%= league.name %></h2>
    <p><%= league.city %>, <%= league.country %></p>
  <% end %>
</div>
